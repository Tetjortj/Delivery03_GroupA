using UnityEngine;
using UnityEngine.UI;
using TMPro;
using System.Collections.Generic;

public class ShopUIManager : MonoBehaviour
{
    // public Inventory shopInventory;
    // public Transform shopContainer;
    // public GameObject shopSlotPrefab;

    // void Start()
    // {
    //     LoadShopItems();
    // }

    // void LoadShopItems()
    // {
    //     foreach (Item item in shopInventory.items)
    //     {
    //         // Crear un nuevo `ShopSlot`
    //         GameObject newSlot = Instantiate(shopSlotPrefab, shopContainer);

    //         // Asignar el �cono del �tem
    //         newSlot.transform.Find("ItemImage").GetComponent<Image>().sprite = item.icon;

    //         // Asignar la cantidad (por ahora 1, pero puedes hacer que venga del inventario)
    //         newSlot.transform.Find("Quantity (TMP)").GetComponent<TMP_Text>().text = "x1";
    //     }
    // }
}
