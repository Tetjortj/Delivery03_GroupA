using UnityEngine;

public class InventoryManager : MonoBehaviour
{
    public addItem(string itemName, int quantity, int cost, Sprite sprite)
    {
        Debug.Log("itemName = " + itemName + "quantinty = " + quantity + "cost = " + cost + "itemSprite = " + sprite);
    }
}
